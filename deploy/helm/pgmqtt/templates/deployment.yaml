apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pgmqtt.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pgmqtt.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      {{- include "pgmqtt.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "pgmqtt.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "pgmqtt.fullname" . }}
      automountServiceAccountToken: {{ .Values.automountServiceAccountToken }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      {{- if .Values.hostNetwork }}
      hostNetwork: true
      {{- end }}
      {{- with .Values.dnsPolicy }}
      dnsPolicy: {{ . }}
      {{- end }}
      {{- with .Values.dnsConfig }}
      dnsConfig:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.runtimeClassName }}
      runtimeClassName: {{ . }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: pgmqttd
          image: "{{ .Values.image.repository }}{{ if .Values.image.digest }}@{{ .Values.image.digest }}{{ else }}:{{ .Values.image.tag | default .Chart.AppVersion }}{{ end }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: PGMQTT_DATABASE_URL
              {{- if .Values.database.existingSecret }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.existingSecret }}
                  key: {{ .Values.database.existingSecretKey }}
              {{- else }}
              value: {{ required "database.url is required when database.existingSecret is empty" .Values.database.url | quote }}
              {{- end }}
            - name: PGMQTT_TCP_ADDR
              value: {{ if .Values.listeners.tcp.enabled }}":{{ .Values.listeners.tcp.port }}"{{ else }}""{{ end }}
            - name: PGMQTT_WS_ADDR
              value: {{ if .Values.listeners.ws.enabled }}":{{ .Values.listeners.ws.port }}"{{ else }}""{{ end }}
            {{- with .Values.listeners.ws.allowedOrigins }}
            - name: PGMQTT_WS_ALLOWED_ORIGINS
              value: {{ join "," . | quote }}
            {{- end }}
            - name: PGMQTT_SERVICE_HOST
              value: {{ include "pgmqtt.serviceHost" . | quote }}
            - name: PGMQTT_SERVICE_PORT
              value: {{ .Values.listeners.tcp.port | quote }}
            - name: PGMQTT_SERVICE_WS_PORT
              value: {{ .Values.listeners.ws.port | quote }}
            # Numeric env values pipe through `int` before quote because helm
            # / sprig render large integers (≥ 16M-ish) in Go's %v scientific
            # notation ("1.6777216e+07"), which the broker's strconv.Atoi
            # rejects. `int` forces decimal-integer rendering. Smaller knobs
            # (bcryptCost, replicaCount) don't trip the format threshold but
            # are cast for consistency.
            - name: PGMQTT_RECEIVE_MAXIMUM
              value: {{ int .Values.v5.receiveMaximum | quote }}
            - name: PGMQTT_KEEPALIVE_MAX_SEC
              value: {{ int .Values.v5.keepaliveMaxSec | quote }}
            - name: PGMQTT_BCRYPT_COST
              value: {{ int .Values.operator.bcryptCost | quote }}
            - name: PGMQTT_JANITOR_INTERVAL_MS
              value: {{ int .Values.janitor.intervalMs | quote }}
            - name: PGMQTT_MAX_QUEUED_DELIVERIES_PER_CLIENT
              value: {{ int .Values.limits.maxQueuedDeliveriesPerClient | quote }}
            - name: PGMQTT_MAX_CONNECTIONS
              value: {{ int .Values.limits.maxConnections | quote }}
            - name: PGMQTT_MAX_INBOUND_MSGS_PER_SEC
              value: {{ int .Values.limits.maxInboundMsgsPerSec | quote }}
            - name: PGMQTT_MAX_CONNECTS_PER_IP_PER_SEC
              value: {{ int .Values.limits.maxConnectsPerIPPerSec | quote }}
            - name: PGMQTT_MAX_AUTH_FAILURES_PER_IP_PER_MIN
              value: {{ int .Values.limits.maxAuthFailuresPerIPPerMin | quote }}
            - name: PGMQTT_MAX_PACKET_SIZE
              value: {{ int .Values.limits.maxPacketSize | quote }}
            - name: PGMQTT_PG_STATEMENT_TIMEOUT_MS
              value: {{ int .Values.database.statementTimeoutMs | quote }}
            - name: PGMQTT_LOG_FORMAT
              value: {{ .Values.logging.format | quote }}
            {{- if .Values.auth.allowAnonymous }}
            - name: PGMQTT_ALLOW_ANONYMOUS
              value: "true"
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            - name: PGMQTT_METRICS_ADDR
              value: {{ if .Values.metrics.enabled }}":{{ .Values.metrics.port }}"{{ else }}""{{ end }}
          {{- with .Values.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            {{- if .Values.listeners.tcp.enabled }}
            - name: mqtt
              containerPort: {{ .Values.listeners.tcp.port }}
              protocol: TCP
            {{- end }}
            {{- if .Values.listeners.ws.enabled }}
            - name: mqtt-ws
              containerPort: {{ .Values.listeners.ws.port }}
              protocol: TCP
            {{- end }}
            {{- if .Values.metrics.enabled }}
            - name: metrics
              containerPort: {{ .Values.metrics.port }}
              protocol: TCP
            {{- end }}
          {{- if .Values.probes.enabled }}
          # Liveness probe on the MQTT TCP listener — failing this
          # restarts the pod. Metrics-port httpGet would more cleanly
          # distinguish "process alive" from "PG wedged"; the
          # /healthz/live and /healthz/ready endpoints are exposed on
          # the metrics port for operators who want to opt in (set
          # probes.useHTTP=true). Default stays TCP for predictable
          # tier3 behaviour.
          {{- if .Values.probes.useHTTP }}
          livenessProbe:
            httpGet:
              path: /healthz/live
              port: metrics
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds | default 5 }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds | default 1 }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold | default 3 }}
            successThreshold: {{ .Values.probes.liveness.successThreshold | default 1 }}
          readinessProbe:
            httpGet:
              path: /healthz/ready
              port: metrics
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds | default 2 }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds | default 5 }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds | default 2 }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold | default 3 }}
            successThreshold: {{ .Values.probes.readiness.successThreshold | default 1 }}
          {{- else }}
          livenessProbe:
            tcpSocket:
              port: {{ if .Values.listeners.tcp.enabled }}mqtt{{ else }}mqtt-ws{{ end }}
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds | default 5 }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds | default 1 }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold | default 3 }}
            successThreshold: {{ .Values.probes.liveness.successThreshold | default 1 }}
          readinessProbe:
            tcpSocket:
              port: {{ if .Values.listeners.tcp.enabled }}mqtt{{ else }}mqtt-ws{{ end }}
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds | default 2 }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds | default 5 }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds | default 1 }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold | default 3 }}
            successThreshold: {{ .Values.probes.readiness.successThreshold | default 1 }}
          {{- end }}
          # startupProbe gives migrations + listener bind room before
          # liveness can fire. TCP-based; works whether useHTTP is on
          # or off.
          startupProbe:
            tcpSocket:
              port: {{ if .Values.listeners.tcp.enabled }}mqtt{{ else }}mqtt-ws{{ end }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds | default 5 }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold | default 30 }}
          {{- if .Values.lifecycle.preStop }}
          lifecycle:
            preStop:
              {{- toYaml .Values.lifecycle.preStop | nindent 14 }}
          {{- end }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- with .Values.extraVolumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.extraVolumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.affinity .Values.podAntiAffinity.enabled }}
      affinity:
        {{- with .Values.affinity }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.podAntiAffinity.enabled }}
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: {{ .Values.podAntiAffinity.weight }}
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    {{- include "pgmqtt.selectorLabels" . | nindent 20 }}
        {{- end }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
