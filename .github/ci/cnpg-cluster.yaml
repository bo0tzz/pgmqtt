apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pgmqtt-pg
  namespace: mqtt
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: pgmqtt
      owner: pgmqtt
